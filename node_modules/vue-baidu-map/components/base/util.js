'use strict';

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.getPosition = exports.toKebabCase = exports.checkType = exports.isPoint = undefined;

var _factory = require('./factory');

var isPoint = exports.isPoint = function isPoint(obj) {
  return obj.lng && obj.lat;
};
var checkType = exports.checkType = function checkType(val) {
  return Object.prototype.toString.call(val).slice(8, -1);
};
var toKebabCase = exports.toKebabCase = function toKebabCase(str) {
  return str.replace(/[A-Z]/g, function (letter) {
    return '-' + letter.toLowerCase();
  }).replace(/^-/, '');
};

var getPosition = exports.getPosition = function getPosition(BMap, point) {
  return isPoint(point) ? (0, _factory.createPoint)(BMap, point) : point;
};