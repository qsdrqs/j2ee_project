<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.dingpang.test.mapper.UserMapper">
	<select id="findById" parameterType="int"
		resultType="grp.wudi.j2ee.entity.User">
		SELECT
		user_id,
		user_name,
		user_sex,
		user_account
		FROM
		user
		WHERE user_id = #{id}
	</select>
	
	<select id="findByAccount" resultType="grp.wudi.j2ee.entity.User">
		SELECT
		user_id,
		user_name,
		user_sex,
		user_account
		FROM
		user
		WHERE user_account LIKE '%${userAccount}%'
	</select>
	
	<insert id="addUser" keyProperty="userId" useGeneratedKeys="true"
		parameterType="grp.wudi.j2ee.entity.User">
		INSERT INTO user(user_name,user_sex,user_account)
		VALUES(#{userName},#{userSex},#{userAccount})
	</insert>
	
	<update id="update"
		parameterType="grp.wudi.j2ee.entity.User">
		UPDATE user
		SET user_name=#{userName}, user_sex=#{userSex},user_account = #{userAccount}
		WHERE user_id = #{userId}
	</update>
</mapper>